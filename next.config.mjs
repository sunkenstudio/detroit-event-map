/** @type {import('next').NextConfig} */
export default {
  eslint: {
    // ESLint during builds is enabled by default.
    // Set this option to false to disable ESLint during builds.
    ignoreDuringBuilds: true,
  },
};
